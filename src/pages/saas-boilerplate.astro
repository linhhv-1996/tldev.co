---
import BlankLayout from '../layouts/BlankLayout.astro';
import { Button } from '@/components/ui/button';

// --- CONFIG ---
const AFFILIATE_LINK = "https://shipfa.st/?via=your-affiliate-id"; // Thay Link của bạn vào đây
const DISCOUNT_TEXT = "Save ~40h of setup time";
---

<BlankLayout 
  title="ShipFast Review: How I Built a Complex 'Reddit Finder' SaaS in Just 48 Hours"
  description="Stop wasting 2 months on setup. A real case study on buying speed and freedom."
>
  
  <main class="min-h-screen bg-background text-foreground font-sans pb-0"> {/* Thêm padding-bottom để không bị Sticky Bar che nội dung cuối */}
    <div class="max-w-3xl mx-auto px-5 py-16 md:py-16">
      
      {/* --- HEADER --- */}
      <header class="mb-10 text-center">
        <h1 class="text-4xl md:text-5xl font-extrabold tracking-tight mb-6 leading-tight">
          ShipFast Review: How I Built a Complex "Reddit Finder" SaaS in Just 48 Hours <br class="hidden md:block"/> (Real Case Study)
        </h1>
        <p class="text-xl text-muted-foreground leading-relaxed">
          Stop wasting 2 months of your life just setting up Auth & Payment. 
          Here is how I used <span class="text-foreground font-semibold">$200</span> to buy freedom and speed.
        </p>
      </header>

      {/* --- IMAGE 1 --- */}
      <figure class="my-10 group">
        <div class="rounded-xl overflow-hidden shadow-2xl border border-border bg-muted transition-transform duration-500 hover:scale-[1.01]">
            <img 
              src="/leadscot.png" 
              alt="Reddit Lead Finder Interface" 
              class="w-full h-auto object-cover"
              loading="eager" 
            />
        </div>
        <figcaption class="text-center text-xs text-muted-foreground mt-3 italic">
          (This is the SaaS I coded in a weekend thanks to ShipFast.)
        </figcaption>
      </figure>

      <div class="space-y-12 text-lg leading-relaxed">
        
        {/* --- SECTION 1 --- */}
        <section>
          <h2 class="text-2xl md:text-3xl font-bold mb-4">Why do I (and you) always fail at the start?</h2>
          <p class="mb-4">
            As developers, I bet you have tons of ideas in your head. But let's be honest, how many dead projects are currently rotting in your <code>Projects</code> folder?
          </p>
          <ul class="list-disc pl-6 space-y-2 mb-6 marker:text-primary">
            <li><strong>Project A:</strong> Abandoned because NextAuth configuration kept breaking.</li>
            <li><strong>Project B:</strong> Gave up because Stripe docs gave you a headache, and webhooks wouldn't fire.</li>
            <li><strong>Project C:</strong> Finished the Backend, but too lazy to build the Frontend UI because it takes forever.</li>
          </ul>
          <p>
            We get stuck in what's called <strong>"Setup Hell"</strong>. We spend 90% of our time on repetitive boilerplate tasks and only have 10% energy left for the actual features.
          </p>
          <p class="font-medium border-l-4 border-primary pl-4 italic">
            That's why I decided to "bite the bullet" and pay for <strong>ShipFast</strong>.
          </p>
        </section>

        <hr class="border-border" />

        {/* --- SECTION 2 --- */}
        <section>
          <h2 class="text-2xl md:text-3xl font-bold mb-4">Real Combat Case Study: Reddit Lead Finder</h2>
          <p class="mb-4">
            To test if this boilerplate was worth the money, I used it to build a Reddit Lead Finder tool. This was a "tough" app that needed heavy data processing, not just a simple static landing page.
          </p>
          <p class="mb-4">
            If I had coded it from scratch, I calculated it would cost me:
          </p>
          <ul class="list-disc pl-6 space-y-2 mb-6 marker:text-primary">
            <li>3 days for NextJS + Tailwind + Component setup.</li>
            <li>5 days for Database + API Routes setup.</li>
            <li><strong>7 days struggling with Stripe/Lemon Squeezy Webhooks</strong> (The most painful part).</li>
            <li><strong>Total:</strong> ~1 month just to reach a basic MVP.</li>
          </ul>

          <h3 class="text-xl font-bold mt-8 mb-3">But with ShipFast, my process was shortened to:</h3>
          <ol class="list-decimal pl-6 space-y-3 mb-6">
            <li><code>git clone</code> the project (Supports both App Router and Page Router).</li>
            <li>Fill in API Keys into the <code>.env</code> file (Google Auth, Database, Resend, Stripe).</li>
            <li><strong>BOOM!</strong> I instantly had a full-featured SaaS: Login, Payments, Transactional Emails, Database connection.</li>
            <li>I spent the next full 48 hours purely writing the <code>fetchRedditData()</code> logic for the product.</li>
          </ol>

          {/* --- VIDEO EMBED --- */}
          <div class="my-8 rounded-xl overflow-hidden shadow-2xl border border-border bg-black group">
            <div class="aspect-video w-full relative">
              <iframe 
                class="absolute top-0 left-0 w-full h-full"
                src="https://www.youtube.com/embed/W3Pb3v_GpoE" 
                title="ShipFast Demo"
                frameborder="0" 
                allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" 
                referrerpolicy="strict-origin-when-cross-origin"
                allowfullscreen
              ></iframe>
            </div>
          </div>
        </section>

        <hr class="border-border" />

        {/* --- SECTION 3 --- */}
        <section>
          <h2 class="text-2xl md:text-3xl font-bold mb-4">The Project Failed Miserably, but I Still Profited?</h2>
          <p class="mb-4">
            Hard truth: I ran Ads for this Reddit tool and... no one bought it. <strong>The project is dead.</strong>
          </p>
          <p class="mb-4">But guess what? I am <strong>EXTREMELY HAPPY</strong>.</p>
          <ul class="list-disc pl-6 space-y-2 mb-6 marker:text-primary">
            <li>If I coded from scratch (1 month) &rarr; Launch fail &rarr; I lose 1 month of my life. I'd be bitter and might quit.</li>
            <li>Used ShipFast (2 days) &rarr; Launch fail &rarr; I only lost a weekend.</li>
          </ul>
          <p class="font-bold text-xl mb-4">
            The real value of ShipFast isn't just clean code, it's the license to FAIL and RETRY extremely fast.
          </p>
          <p>
            I've archived that project, cloned the ShipFast repo again, and am now building my next SaaS. Same codebase, used for N projects forever.
          </p>
        </section>

        <hr class="border-border" />

        {/* --- SECTION 4 --- */}
        <section>
          <h2 class="text-2xl md:text-3xl font-bold mb-4">Dissecting the $200 Code: What do Devs need to know?</h2>
          <p class="mb-6">I inspected the code closely before using it. Here is my honest review of the Tech Stack:</p>
          
          <div class="grid gap-4 md:grid-cols-2 mb-6">
            <div class="bg-card border p-4 rounded-lg shadow-sm hover:shadow-md transition-shadow">
              <h4 class="font-bold mb-2">🚀 Framework</h4>
              <p class="text-sm text-muted-foreground">Next.js (Flexible choice between App Router or Pages Router).</p>
            </div>
            <div class="bg-card border p-4 rounded-lg shadow-sm hover:shadow-md transition-shadow">
              <h4 class="font-bold mb-2">🎨 Styling</h4>
              <p class="text-sm text-muted-foreground">Tailwind CSS + DaisyUI (Easy theming, no lock-in to author's style).</p>
            </div>
            <div class="bg-card border p-4 rounded-lg shadow-sm hover:shadow-md transition-shadow">
              <h4 class="font-bold mb-2">💳 Payment (Best Value)</h4>
              <p class="text-sm text-muted-foreground">Stripe & Lemon Squeezy integrated. Handles Subscriptions, Webhooks, and Customer Portals.</p>
            </div>
            <div class="bg-card border p-4 rounded-lg shadow-sm hover:shadow-md transition-shadow">
              <h4 class="font-bold mb-2">📧 Emails</h4>
              <p class="text-sm text-muted-foreground">Uses <strong>Resend</strong> - clean API, high deliverability, beautiful templates included.</p>
            </div>
          </div>

          <p class="mb-2"><strong>👍 Pros:</strong> Clean code, easy to understand. Doesn't use too much bloatware. Easy to customize.</p>
          <p class="mb-4"><strong>👎 Cons:</strong> If you are a TypeScript strict mode purist, some usage of <code>any</code> might bug you. But hey, this code is for making money fast, not for a CS thesis.</p>
        </section>

        <hr class="border-border" />

        {/* --- MAIN CTA SECTION --- */}
        <section class="bg-secondary/20 p-8 rounded-3xl border border-primary/20 text-center relative overflow-hidden">
          <div class="absolute inset-0 bg-gradient-to-tr from-primary/5 to-transparent pointer-events-none"></div>
          
          <h2 class="text-3xl md:text-4xl font-extrabold mb-6">Conclusion: Should you buy it?</h2>
          
          <div class="grid md:grid-cols-2 gap-6 text-left mb-10 max-w-2xl mx-auto">
            <div class="bg-background/50 p-4 rounded-xl border border-red-200 dark:border-red-900/50">
              <strong class="text-red-500 text-lg flex items-center gap-2">
                <span>⛔</span> DO NOT BUY IF:
              </strong>
              <ul class="list-disc pl-6 mt-2 text-sm text-muted-foreground space-y-1">
                <li>You are a student learning basics.</li>
                <li>You enjoy "reinventing the wheel".</li>
              </ul>
            </div>
            
            <div class="bg-background/50 p-4 rounded-xl border border-green-200 dark:border-green-900/50">
              <strong class="text-green-600 text-lg flex items-center gap-2">
                <span>✅</span> BUY IMMEDIATELY IF:
              </strong>
              <ul class="list-disc pl-6 mt-2 text-sm text-muted-foreground space-y-1">
                <li>You are an <strong>Indie Hacker</strong>.</li>
                <li>You value your time > $200.</li>
                <li>You want to ship FAST.</li>
              </ul>
            </div>
          </div>

          <p class="mb-8 font-medium text-lg">
            Don't let project setup block your money flow.
          </p>
          
          <div class="flex flex-col items-center gap-3">
            <a href={AFFILIATE_LINK} target="_blank" rel="noopener noreferrer" class="w-full md:w-auto">
              {/* BIG DESKTOP CTA BUTTON */}
              <Button 
                size="lg" 
                className="cursor-pointer w-full md:w-auto text-lg px-10 py-8 rounded-full font-bold shadow-xl shadow-primary/30 hover:shadow-2xl hover:scale-105 active:scale-95 transition-all duration-300 bg-primary hover:bg-primary/90 text-primary-foreground border-2 border-transparent hover:border-white/20"
              >
                ⚡ GET SHIPFAST & START SHIPPING
              </Button>
            </a>
            <p class="text-sm text-muted-foreground bg-background/50 px-3 py-1 rounded-full border">
              🎁 {DISCOUNT_TEXT}
            </p>
          </div>
        </section>

      </div>
    </div>
    
    <div class="text-center py-10 text-sm text-muted-foreground border-t mt-12">
      © {new Date().getFullYear()} TLDev. All rights reserved.
    </div>

    {/* --- STICKY MOBILE CTA BAR --- */}
    <div class="fixed bottom-0 left-0 right-0 p-4 bg-background/80 backdrop-blur-lg border-t border-border z-50 md:hidden pb-safe animate-in slide-in-from-bottom-full duration-500">
      <div class="flex items-center justify-between gap-4 max-w-md mx-auto">
        <div class="flex flex-col">
          <span class="font-bold text-sm">ShipFast Boilerplate</span>
          <span class="text-xs text-muted-foreground line-through">$299 value</span>
        </div>
        <a href={AFFILIATE_LINK} target="_blank" rel="noopener noreferrer" class="flex-1">
          <Button className="cursor-pointer w-full font-bold shadow-lg bg-primary text-primary-foreground hover:bg-primary/90 active:scale-95 transition-transform">
            Get it now 👉
          </Button>
        </a>
      </div>
    </div>

  </main>
</BlankLayout>

<style>
  /* Tiện ích để hỗ trợ Safe Area trên iPhone đời mới (tai thỏ) */
  .pb-safe {
    padding-bottom: env(safe-area-inset-bottom, 20px);
  }
</style>
